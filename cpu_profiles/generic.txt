# Generic CPU profile, not tied to any real chip.
# Used as the default when no -cpu is given.
#
# Format: `key = value`, one per line. `#` starts a comment. Units are
# abstract "cycles"; only relative magnitude matters for scheduling
# decisions, so these don't need to be calibrated against a real clock.

num_registers = 16

add.latency     = 3.0
add.throughput  = 1.0

mul.latency     = 4.0
mul.throughput  = 1.0

div.latency     = 12.0
div.throughput  = 6.0

load.latency    = 3.0
load.throughput = 1.0

store.latency    = 1.0
store.throughput = 1.0
